# Runtime dependencies for the Azure App Service (Oryx) deploy.
# Mirrors the [project].dependencies in pyproject.toml. The trailing `.`
# makes pip run `pip install .` so the src-layout `momdiary` package is
# installed into site-packages (required for `python -m uvicorn momdiary.main:app`).

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.4
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
azure-identity>=1.17
azure-monitor-opentelemetry>=1.6
agent-framework-core==1.0.0rc6
agent-framework-azure-ai==1.0.0rc6
structlog>=24.1
clerk-backend-api>=1.5.0,<2.0.0
svix>=1.30.0,<2.0.0
PyJWT[crypto]>=2.9
httpx>=0.27
tzdata>=2024.1; sys_platform == 'win32'
# Tracks outbound httpx calls (OpenAI/Brave/Clerk) as App Insights dependencies.
# NOT auto-activated by the Azure Monitor distro, so it's enabled explicitly in
# observability/logging.py. Version MUST match the OpenTelemetry release bundled
# by azure-monitor-opentelemetry above (currently 0.61b0); leaving it unpinned
# pulls a newer OTel core that conflicts with the distro's pins.
opentelemetry-instrumentation-httpx==0.61b0


# Not declared in pyproject but required at runtime: code uses pydantic EmailStr.
email-validator>=2.0

# Install the local package itself (src/momdiary) into site-packages.
.
